<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--</activity>-->
        <!--<activity android:name=".RegisterActivity"-->
            <!--android:parentActivityName=".MainActivity">-->
            <!--&lt;!&ndash; The meta-data tag is required if you support API level 15 and lower &ndash;&gt;-->
            <!--<meta-data-->
                <!--android:name="android.support.PARENT_ACTIVITY"-->
                <!--android:value=".MainActivity" />-->

        <!--</activity>-->
        <!--<activity android:name=".SignUpActivity"-->
            <!--android:parentActivityName=".MainActivity">-->
            <!--&lt;!&ndash; The meta-data tag is required if you support API level 15 and lower &ndash;&gt;-->
            <!--<meta-data-->
                <!--android:name="android.support.PARENT_ACTIVITY"-->
                <!--android:value=".MainActivity" />-->

        <!--<activity android:name=".NotesActivity"></activity>-->
        <activity android:name=".ListDataActivity"></activity>
        <activity android:name=".SignUpActivity"  ></activity>
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".EditDataActivity"></activity>
        <activity android:name=".MainAccount"></activity>
        <activity android:name=".congratulationActivity"></activity>
        <activity android:name=".AccountManagement"></activity>
        <activity android:name=".CalenderHelper"></activity>
        <activity android:name=".NotesActivity"></activity>
        
        
    </application>

</manifest>
